function counter() {    
        for (var num = 0; num <= 5; num++) {        
            console.log(num);    
        }node
    }
    counter();    // run the function
    counter();    // run the function again
    